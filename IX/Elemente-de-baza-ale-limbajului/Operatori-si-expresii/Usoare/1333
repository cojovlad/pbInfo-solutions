#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int main() {
    double B, b, l;
    cin >> B >> b >> l;

    double d = sqrt(l*l + B*b);

    cout << fixed << setprecision(5) << d;
    return 0;
}