#include <iostream>
#include <iomanip>
#include <cmath>

int main() {
    double r;
    std::cin >> r;

    double area = M_PI * r * r;
    double perimeter = 2 * M_PI * r;

    std::cout << std::fixed << std::setprecision(6)
              << area << " " << perimeter;

    return 0;
}
