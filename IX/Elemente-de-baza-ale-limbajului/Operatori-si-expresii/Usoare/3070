#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    int P = 2 * (a + b);
    int A = a * b;
    int D = a * a + b * b;

    cout << P << " " << A << " " << D;
    return 0;
}